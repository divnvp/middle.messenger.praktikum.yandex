{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "tsc && vite build",
    "start": "npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.71.1",
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  },
  "dependencies": {
    "vite-plugin-handlebars": "^2.0.0"
  }
}
